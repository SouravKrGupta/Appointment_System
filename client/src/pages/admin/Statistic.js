import React from 'react';
import Layout from '../../components/Layout';

const Statistic = () => {
    return (
        <Layout>
      <h3 className="text-center">Usersjjjjjjjjj List</h3>
      {/* <Table columns={columns} dataSource={users} /> */}
    </Layout>
    );
};

export default Statistic;